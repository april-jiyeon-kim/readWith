extends layouts/main
include mixins/bookBlock
block content
    .flex.m-4
        each item in books
            +bookBlock({
                id:item.id,
                title:item.title,
                author:item.author,
                description:item.description,
                thumbnail:item.thumbnail
            }) 