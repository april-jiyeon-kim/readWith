extends layouts/main
include mixins/bookBlock

block content
    .search__header
        h3 Searching for: #{searchingBy}
    .search__books
        each item in books
            +bookBlock({
                thumbnail:item.thumbnail,
                title:item.title,
                id:item.id
            }) 